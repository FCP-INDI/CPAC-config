Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu/trusty64"
  config.vm.provision :shell, path: "cpac_provision_trusty64.sh"
  config.vm.provider "virtualbox" do |v|
     v.memory = 1024
  end
end
